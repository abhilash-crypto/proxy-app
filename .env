# Port the proxy server listens on
PORT=3000

# Default upstream target (used if no route-specific target matches)
DEFAULT_TARGET=http://localhost:8080

# Comma separated list of allowed origins for CORS. Use * to allow all
ALLOWED_ORIGINS=*

# Set to true to log every proxied request
ENABLE_REQUEST_LOGGING=true
